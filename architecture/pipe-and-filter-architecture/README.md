# Pipe-and-Filter Architecture

Processes data through a chain of independent filters connected by pipes. Each filter transforms its input into output, making processing stages reusable and composable.

