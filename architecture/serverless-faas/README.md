# Serverless / Function-as-a-Service (FaaS)

Runs small functions on demand in a managed platform, with infrastructure scaling handled by the provider. Functions are commonly triggered by HTTP requests, schedules, or events.

