# Hexagonal Architecture (Ports & Adapters)

Places the application core behind ports—interfaces for inputs and outputs—and uses adapters for HTTP, databases, queues, and other technologies. The core remains independent of infrastructure.

