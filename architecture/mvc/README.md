# Model-View-Controller (MVC)

Separates domain data and rules (model), the user interface (view), and input/request handling (controller). It keeps presentation concerns distinct from application behavior.

