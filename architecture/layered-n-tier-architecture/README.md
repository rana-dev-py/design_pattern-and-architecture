# Layered (N-Tier) Architecture

Organizes an application into responsibility-based layers, such as presentation, business logic, and data access. Each layer typically depends only on the layer beneath it.

