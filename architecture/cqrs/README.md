# Command Query Responsibility Segregation (CQRS)

Uses separate models or paths for commands that change state and queries that read state. This can optimize complex read and write needs independently.

