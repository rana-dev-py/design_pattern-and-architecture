# Factory Method

Defines an interface for creating an object while allowing subclasses or implementations to choose the concrete type. It separates object creation from the code that uses the object.

